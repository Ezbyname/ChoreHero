// Native variant (Metro-only — see supabaseAuthPlatform.ts). Native has no
// browser URL for supabase-js to parse a session out of, so this stays
// false there.
export const detectSessionInUrl = false;
